/**
 * Show this component when the search address was directly copy&pasted
 * requeired for modal dialog to function without weird UI logic
 * (as per react router's spec)
 */

import React from "react";

export default function SearchResult() {
  return <div>Show Search Results</div>;
}
